server:
  host: "0.0.0.0"
  port: 8080

appId: -1
secret: "SECRET_KEY"
distance: 1000

vk:
  serviceKey: "SERVICE_KEY"
  baseUrl: "hhh"

database:
  host: localhost
  port: 5432
  user: postgres
  password: postgres
  name: 'ne-noy'
  sslmode: disable
  timezone: "UTC"

grpc:
  attachmentServiceAddr: "localhost:9090"